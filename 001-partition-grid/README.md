# 2026.3.26 等和矩阵分割

## 题目内容

给一个m*n的矩阵，按任意行列分割成两个矩阵，要求两个矩阵非空。

若两个矩阵满足：

- 和相同
- 和不同时，两个矩阵中去掉一个元素后和相同
- 若去掉元素，去掉元素的那个矩阵剩余元素连通不间断

则返回true，否则返回false

## 解决方法

个人进行了暴力求解，讨论每种情况并做判断，时间复杂度过高无法通过检测



官方答案中，进行了一个将矩阵旋转90度的操作，只要**旋转三次**，就只用判断行不需要判断列了，而且每次可以都只在上半部分的矩阵进行判断。